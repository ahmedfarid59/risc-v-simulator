  .text
  .global _start

_start:
  ADDI x1, x0, 100        # x1 = 100
  ADDI x2, x0, 200        # x2 = 200
  LW x3, 0(x1)            # x3 = Mem[x1] (load from memory)
  SW x3, 4(x2)            # Store x3 to memory at address x2 + 4
  LBU x4, 0(x1)           # Load byte unsigned from memory

  FENCE                   # Ensure memory operations are completed

  # Test Case Complete
  .end
